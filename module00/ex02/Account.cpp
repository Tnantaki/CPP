#include "Account.hpp"

int	main(void)
{
	// std::string	cmd;
	// PhoneBook	book;

	// book.open_phonebook();
	// while (1)
	// {
	// 	std::cout << "> ";
	// 	getline(std::cin, cmd);
	// 	if (cmd.compare("ADD") == 0)
	// 		book.add_cont();
	// 	else if (cmd.compare("SEARCH") == 0)
	// 		book.search_cont();
	// 	else if (cmd.compare("EXIT") == 0)
	// 		break ;
	// }
	return (0);
}