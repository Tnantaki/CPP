#include <iostream>
#include <array>
#include <stdexcept>

// template<typename T, size_t N>
// void	print(std::array<T, N> const& var)
// {
// 	for (int i = 0; i < N; i++)
// 		std::cout << var[i] << std::endl;
// }

int	main(void)
{
	// print(var2);
	// std::cout << "Hello" << std::endl;
	// int	*a = new int;
	// try {
	// 	a[0] = 20;
	// 	a[1] = 30;
	// }
	// catch (std::out_of_range& e)
	// {
	// 	std::cerr << "Error" << e.what() << std::endl;
	// }
	// print(var);
	// int	*a = new int[1];
	// std::cout << a << std::endl;
	// std::array<int, -1>	var1;
	// std::cout << "size: " << var1.size() << std::endl;
	// var1[0] = 20;
	// std::cout << var1[0] << std::endl;
	// std::array<int, 6>	var2;
	// for (int i = 0; i < 5; i++)
	// 	var1[i] = 10 * (i + 1);
	// for (int i = 0; i < 5; i++)
	// 	var2[i] = 10 * (i + 2);
	// var1 = var2;
	// print(var1);
	// print(var2);
	// unsigned int	var = 20;
	// std::cout << sizeof(long int) << std::endl;
	// std::cout << static_cast<long int>(var) <<std::endl;
	// std::string	str;
// 	int	*a = new int[20];
// 	for (int i = 0; i < 20; i++)
// 		a[i] = (i + 1);
// 	for (int i = 0; i < 20; i++)
// 		std::cout << a[i] << ", ";
// 	std::cout << std::endl;
// 	delete [] a;
// 	a = NULL;
	std::cout << "Hello\n";
}
